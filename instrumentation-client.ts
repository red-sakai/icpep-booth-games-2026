export function onRouterTransitionStart(
  _url: string,
  _navigationType: "push" | "replace" | "traverse",
) {
  // Intentionally left blank. This file satisfies Next.js client instrumentation resolution.
}
